.dart_tool/

# A library: consumers resolve their own versions.
pubspec.lock

# Prebuilt native libraries. tool/build_binaries.dart fills this before
# publishing. They ship on pub.dev and are not committed.
/binary/

/.zig-cache/
/zig-out/
